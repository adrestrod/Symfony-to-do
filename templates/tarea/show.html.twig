{% extends 'base.html.twig' %}

{% block title %}Tarea{% endblock %}

{% block body %}
    <h1>Tarea</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tarea.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ tarea.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('index') }}" class="btn btn-primary mg-3">Volver</a>

    <a href="{{ path('tarea_edit', {'id': tarea.id}) }}" class="btn btn-warning mg-3">Editar</a>

    {{ include('tarea/_delete_form.html.twig') }}
{% endblock %}
